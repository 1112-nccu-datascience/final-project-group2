[Merge_data]
將./data/中的檔案合併成merged_data.csv的 R code

[Feature Extraction]
將merged_data.csv轉換成Features_ver1.csv的 R code
請使用此資料進行建模測試
需特別注意:
1. genre_id因資料非常不平衡，最後併組成A,B,O三組
2. ordinal type的features的missing value(包括空字串以及N/A)一律以補成'other'
3. numeric type的features的missing value(包括空字串以及N/A)一律以補成-1
